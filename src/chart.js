export function drawSimulation(canvas,ca){const ctx=canvas.getContext('2d',{alpha:false}),N=ca.N,s=canvas.width/N;ctx.fillStyle='#02030a';ctx.fillRect(0,0,canvas.width,canvas.height);for(let y=0;y<N;y++)for(let x=0;x<N;x++)if(ca.cells[y*N+x]){const t=(x+y+ca.generation*.15)%N/N;ctx.fillStyle=`hsl(${235+55*t} 90% ${60+20*t}%)`;ctx.fillRect(x*s,y*s,Math.ceil(s),Math.ceil(s))}}
function line(ctx,arr,map,color,x,y,w,h){if(arr.length<2)return;ctx.strokeStyle=color;ctx.lineWidth=2;ctx.beginPath();arr.forEach((v,i)=>{const px=x+i*w/(arr.length-1),py=y+h*(1-map(v));i?ctx.lineTo(px,py):ctx.moveTo(px,py)});ctx.stroke()}
export function drawChart(canvas,history){const g=canvas.getContext('2d',{alpha:false}),w=canvas.width,h=canvas.height;g.fillStyle='#050710';g.fillRect(0,0,w,h);g.strokeStyle='#242a48';for(const y of[28,145,180,330]){g.beginPath();g.moveTo(42,y);g.lineTo(w-12,y);g.stroke()}g.fillStyle='#9ca6c8';g.font='12px system-ui';g.fillText('logarithmische Entwicklung',42,17);g.fillText('Dichteanteile an E(a)²',42,170);const d=history.slice(-600);if(!d.length)return;const la=d.map(r=>Math.log(Math.max(r.a,1e-30))),le=d.map(r=>Math.log(Math.max(r.E,1e-30))),vals=[...la,...le],min=Math.min(...vals),max=Math.max(...vals),norm=v=>(v-min)/(max-min||1);line(g,la,norm,'#8e7dff',42,28,w-54,117);line(g,le,norm,'#70e1b8',42,28,w-54,117);const map=v=>(Math.max(-1,Math.min(1,v))+1)/2;line(g,d.map(r=>r.Or),map,'#ffcf6e',42,180,w-54,150);line(g,d.map(r=>r.Om),map,'#6eb6ff',42,180,w-54,150);line(g,d.map(r=>r.Ol),map,'#ff84c1',42,180,w-54,150);line(g,d.map(r=>r.Ok),map,'#ff8b72',42,180,w-54,150)}
