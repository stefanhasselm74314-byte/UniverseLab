name: UniverseLab G0 three-track governance v1.6 (historical)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  historical-contract:
    runs-on: ubuntu-latest
    steps:
      - name: Historical notice
        run: echo 'Superseded by the alias-corrected v1.7 canonical synchronization contract.'
