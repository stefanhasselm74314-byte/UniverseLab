name: UniverseLab G0 three-track governance v1.7 (historical)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  historical-notice:
    runs-on: ubuntu-latest
    steps:
      - name: Historical contract notice
        run: echo 'Superseded by the stable Background-3A canonical governance contract using validator v1.8.'
