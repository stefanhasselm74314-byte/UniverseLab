# MD2S-R1-L Forensic Recovery v1.1

Status: `FORENSIC_RECOVERY_ONLY`

Physical gate effect: `NONE`

## 1. Identifier correction

The exact string `MD2S-R1-L` is recovered in the surviving repository corpus as the ID of the `legacy_reproduction` track in:

`science/hzt-m0/md2s/R1_SOURCE_RECONSTRUCTION_LEDGER_v0.1.json`

Its role is therefore:

`RECOVERED_AS_LEGACY_REPRODUCTION_TRACK_ID`.

This does **not** mean that an original historical solver artifact with that identifier survived. The original historical solver package/artifact identity remains unrecovered.

## 2. A0 benchmark block

The A0 values remain classified as surviving reported benchmark values and regression targets only:

- `sqrt(K4) * rho_cap = 1.1196329253611`
- `kappa6^2 * lambda_eff / (4 * sqrt(K4)) = 0.8931498683204`
- `K4 * rho_cap^2 = 1.2535778875527`
- `V_W = 0.5318111250097`
- for `K4 = beta = 1`: `R_circle = 0.6661500466003`

They are not promoted to a recovered historical solver transaction.

## 3. BVP definitions

The preserved v0.4 BVP run specification remains a contract definition with `status: NOT_EXECUTED`. Its center, outer, finite-difference and SVD definitions are real surviving specifications, not an executed historical BVP result.

## 4. B1.4K and B1.4L

The historically reported numerical blocks remain:

`E4_UNVERIFIED_HISTORICAL_CHAT_REPORT`.

The legacy branch audit did not recover a primary solver/output/residual artifact for either block.

## 5. Legacy branch audit result

Thirteen legacy `agent/md2s-*` branches were inventoried by branch-vs-main delta. High-value science ledgers and checkpoints were inspected for historical solver provenance.

The audit found three distinct data classes:

1. **legacy reconstruction metadata** — including the surviving `MD2S-R1-L` track label;
2. **current canonical-rebuild derivations** — radial, parameter/flux and junction structures that explicitly do not prove historical A0 identity;
3. **new C1 candidate analytic/numerical diagnostics** — real data for C1, explicitly marked `Historical A0 identity = NOT_CLAIMED`.

None supplies the missing original historical two-sided interface export.

## 6. C1 firewall

The C1 exact analytic anchor contains exact interface values at `x = pi/2`, including `ell=1`, `ell_x=0`, `A_x=0`, `varphi_x=0`, and later C1 branches contain real converged diagnostic Jacobian/backend results.

These data are classified as:

`E6_REBUILD_OR_C1_DATA_NOT_HISTORICAL`.

They may not be used to reconstruct or overwrite the missing historical MD2S-R1-L interface values.

## 7. Missing primary historical sources

The surviving source-reconstruction ledger still classifies as missing or partial:

- exact historical independent radial ODE set;
- historical Hamiltonian constraint/dependency map;
- exact historical cap/localized action;
- exact historical oriented junction conventions;
- complete benchmark definitions/integration conventions for `V_W`, `R_circle`, `Xi_cap`, `lambda_eff`.

## 8. Missing historical two-sided interface export

Still missing:

- `A'_bulk`
- `A'_cap`
- `(L'/L)_bulk`
- `(L'/L)_cap`
- run-bound oriented normal convention
- historical solver artifact
- derivative tables
- residual logs

Therefore:

`EXACT_HISTORICAL_TWO_JUNCTION_REPLAY = NOT_REPRODUCIBLE_FROM_SURVIVING_ARCHIVE`.

## 9. Promotion rule

A numerical value can become `VERIFIED_SOLVER_OUTPUT` only with primary output, code/solver identity, input binding, run identity, residual/convergence evidence and unambiguous model/branch association.

Reconstructed values remain separately classified as `RECONSTRUCTED` and may never masquerade as lost historical values.

## 10. Governance invariants

Unchanged:

- `official_MD2S_solver = NOT_AUTHORIZED`
- `PHYSICAL_BACKGROUND = NOT_ESTABLISHED`
- `K1-D = NOT_RELEASED`
- `K1-E = NOT_ADMISSIBLE`
- `physical_evidence_effect = NONE`
- `physical_gate_effect = NONE`

## 11. Next forensic target

The highest-value remaining target is any still-unmatched export binary or original long MD-2S chat/PDF source that post-dates 2025-12-17 and could contain the missing historical equations, one-sided interface export, B1.4K/B1.4L primary output or residual provenance.
